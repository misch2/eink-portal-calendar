@model Display
@using PortalCalendarServer.Models.DatabaseEntities
@using PortalCalendarServer.Models.POCOs
@using PortalCalendarServer.Services
@inject IDisplayService displayService
@inject IGalleryService galleryService

@{
    var galleries = await galleryService.GetAllGalleriesAsync();
    var currentValue = displayService.GetConfigWithoutDefaults(Model, "gallery_id");
}

<div class="col-md-6 mt-2">
    <label for="gallery_id">Gallery</label>
    <select id="gallery_id" name="gallery_id" class="form-select">
        <option value="">— select a gallery —</option>
        @foreach (var gallery in galleries)
        {
            <option value="@gallery.Id" selected="@(currentValue == gallery.Id.ToString())">
                @gallery.Name (@gallery.Images.Count images)
            </option>
        }
    </select>
</div>

@await Html.PartialAsync("_ConfigCheckbox", new ConfigInputModel
{
    Display = Model, Key = "gallery_hide_descriptions",
    Label = "Hide descriptions",
    ColClass = "col-12", MarginClass = "mt-3"
})
