@* Partial view for EPD color CSS variables *@
@model Dictionary<string, string>?

<style type="text/css">
    :root {
        @if (Model != null)
        {
            foreach (var (colorName, colorValue) in Model)
            {
                @: --epd-@colorName : @colorValue;
            }
        }
    }
</style>
