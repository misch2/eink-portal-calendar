@* Partial view for EPD color CSS variables *@
@model Dictionary<string, string>?

<style type="text/css">
    :root {
        @{
            foreach (var color_name in Model?.Keys) 
            {
                <text>--epd-@color_name : @Model[color_name];
                </text>
            }
        }
    }
</style>
